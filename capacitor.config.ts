import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-react-calc-proyecto-miguel-jamil',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
